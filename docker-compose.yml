version: '3'

services:
  web:
    container_name: django
    build:
      context: .
      dockerfile: Dockerfile
    command: 
      python3 manage.py runserver 0.0.0.0:4000
    volumes:
      - ./django:/code
    ports:
      - "4000:4000"
